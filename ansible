Create /etc/audit/rules.d/MAC-policy.rules with rules for /etc/selinux and /usr/share/selinux; reload auditd.

Ensure MAC policy directories are audited
Log writes/attribute changes to SELinux policy directories.
sudo cat /etc/audit/rules.d/MAC-policy.rules
-w /etc/apparmor/ -p wa -k MAC-policy

-w /etc/apparmor.d/ -p wa -k MAC-policy
