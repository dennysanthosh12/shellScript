2026-02-14T09:18:54.677Z  WARN 1 --- [apiadmin] [oundedElastic-1] org.hibernate.orm.jdbc.error             : Data truncation: Data too long for column 'server_details' at row 1
could not execute statement [Data truncation: Data too long for column 'server_details' at row 1] [insert into integration_status (last_checked,node_name,server_details,server_ip,status,id) values (?,?,?,?,?,?)]; SQL [insert into integration_status (last_checked,node_name,server_details,server_ip,status,id) values (?,?,?,?,?,?)]
2026-02-14T09:19:04.675Z  WARN 1 --- [apiadmin] [oundedElastic-1] org.hibernate.orm.jdbc.error             : HHH000247: ErrorCode: 1406, SQLState: 22001
2026-02-14T09:19:04.675Z  WARN 1 --- [apiadmin] [oundedElastic-1] org.hibernate.orm.jdbc.error             : Data truncation: Data too long for column 'server_details' at row 1
could not execute statement [Data truncation: Data too long for column 'server_details' at row 1] [insert into integration_status (last_checked,node_name,server_details,server_ip,status,id) values (?,?,?,?,?,?)]; SQL [insert into integration_status (last_checked,node_name,server_details,server_ip,status,id) values (?,?,?,?,?,?)]

package com.EIS.TCS.apiadmin.Model;

import java.time.LocalDateTime;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Id;
import lombok.Data;
import lombok.NoArgsConstructor;

@Entity
@Data
@NoArgsConstructor
public class IntegrationStatus {

	@Id
    private int id;

    @Column(name = "node_name", length = 50, nullable = false)
    private String nodeName;
    
    @Column(name = "server_ip", length = 15, nullable = false)
    private String serverIp;

    @Column(name = "status", nullable = false)
    private String status;

    @Column(name = "last_checked", nullable = false)
    private LocalDateTime lastChecked;

    
    private String serverDetails ;   // JSON as String

	public IntegrationStatus(String nodeName,String serverIp, String status, LocalDateTime lastChecked, String response) {
		this.id= (nodeName+serverIp).hashCode();
		this.nodeName = nodeName;
		this.serverIp = serverIp;
		this.status = status;
		this.lastChecked = lastChecked;
		this.serverDetails = response;
	}
    
    
    
}



package com.EIS.TCS.apiadmin.Services;

import java.time.LocalDateTime;
import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.web.reactive.function.client.WebClient;
import com.EIS.TCS.apiadmin.Dto.IntegrationNodeWrapper;
import com.EIS.TCS.apiadmin.Model.IntegrationStatus;
import com.EIS.TCS.apiadmin.Model.NodeEndpoint;
import com.EIS.TCS.apiadmin.Repository.EndpointRepository;
import com.EIS.TCS.apiadmin.Repository.IntegrationStatusRepository;
import reactor.core.publisher.Flux;
import reactor.core.publisher.Mono;
import reactor.core.scheduler.Schedulers;
import tools.jackson.databind.ObjectMapper;

@Service
public class IntegrationPollingService {
	
	private static final ObjectMapper objectMapper = new ObjectMapper();

	@Autowired
	private IntegrationStatusRepository statusRepository;
	
	@Autowired
	private WebClient webClient;
	
	@Autowired
	private EndpointRepository endpointRepo;
	
	public Mono<Void> pollAllNodes() {

        List<NodeEndpoint> endpoints =
                endpointRepo.findByActiveTrue();

        return Flux.fromIterable(endpoints)
                .flatMap(this::callAceApi) 
                .then();
    }
	
	private Mono<Void> callAceApi(NodeEndpoint ep) {

        String url = String.format(
                "%s://%s:%d/apiv2?depth=2",
                ep.getProtocol(), ep.getHost(), ep.getPort());

        return webClient.get()
                .uri(url)
                .retrieve()
                .bodyToMono(IntegrationNodeWrapper.class)
                .flatMap(resp ->
                    Mono.fromRunnable(() -> saveStatus(ep, resp))
                        .subscribeOn(Schedulers.boundedElastic())
                )
                .onErrorResume(ex -> {
                    saveDownStatus(ep, ex);
                    return Mono.empty();
                })
                .then();
    }
	
	private void saveDownStatus(NodeEndpoint ep, Throwable ex) {

	    IntegrationStatus status =
	            new IntegrationStatus(
	                    ep.getHost(),
	                    ep.getHost(),
	                    "DOWN",
	                    LocalDateTime.now(),
	                    null);

	    statusRepository.save(status);
	    System.out.println(ex.getMessage());
	}
	
	public void saveStatus(NodeEndpoint ep,IntegrationNodeWrapper response) {


		String jsonResponse = objectMapper.writeValueAsString(response);
		IntegrationStatus status = new IntegrationStatus(response.getName(),ep.getHost(),"UP",LocalDateTime.now(),jsonResponse);
		statusRepository.save(status);

}
}
