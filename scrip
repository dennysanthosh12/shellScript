FROM rhel:9.6
RUN groupadd -r mqbrkrs
RUN useradd -r -g mqbrkrs -m -d /home/eisuser -s /bin/bash eisuser
RUN mkdir -p /var/mqsi \
    && mkdir -p /var/mqsi/common/errors \
    && mkdir -p /var/mqsi/common/log \
    && mkdir -p /opt/IBM \
	&& mkdir -p /opt/IBM/EndPoint_Public \
	&& mkdir -p /opt/IBM/Keystore \
	&& mkdir -p /opt/IBM/RSAKeystore \
	
COPY ./Broker_Properties /opt/IBM/
ADD ./ace.tar.gz /opt/IBM/

RUN cd /opt/IBM/ace && \
    .ace make registry global accept license silently
	
RUN echo 'source /opt/IBM/ace/server/bin/mqsiprofile' >> /etc/bashrc && \
    echo 'source /opt/IBM/ace/server/bin/mqsiprofile' > /etc/profile.d/ace.sh && \
    chmod +x /etc/profile.d/ace.sh

RUN chown -R eisuser:mqbrkrs /var/mqsi && \
    chmod -R 775 /var/mqsi && \
    chown -R eisuser:mqbrkrs /opt/IBM && \
    chmod -R 775 /opt/IBM

USER eisuser

WORKDIR /var/mqsi/




[eisuser@EISUATEXP09 ~]$ mqsicreateworkdir SERVER
mqsicreateworkdir: Copying sample server.config.yaml to work directory
1 file(s) copied.
Successful command completion.

[eisuser@EISUATEXP09 ~]$ ibmint specify jre --version 8 --work-directory SERVER
BIP15273I: The JRE to be used by this Integration Server has changed. This will take effect next time the Integration Server is started.

[eisuser@EISUATEXP09 ~]$ IntegrationServer --work-dir SERVER --name "$SERVER_NAME" --overrides-directory "$OVERRIDE_PATH"



#!/bin/bash

source /opt/IBM/ace/server/bin/mqsiprofile

# Create work directory only if it doesn't already exist
if [ ! -d "/var/mqsi/${SERVER_NAME}" ]; then
    echo "Creating work directory for ${SERVER_NAME}..."
    mqsicreateworkdir /var/mqsi/${SERVER_NAME}
    ibmint specify jre --version 8 --work-directory /var/mqsi/${SERVER_NAME}
else
    echo "Work directory /var/mqsi/${SERVER_NAME} already exists, skipping creation..."
fi

# Start Integration Server
exec IntegrationServer --work-dir /var/mqsi/${SERVER_NAME} \
    --name "${SERVER_NAME}" \
    --overrides-directory "${OVERRIDE_PATH}"


COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

USER eisuser

ENTRYPOINT ["/entrypoint.sh"]

podman run -d \
    -e SERVER_NAME=MYSERVER \
    -e OVERRIDE_PATH=/mnt/overrides \
    -v /host/var/mqsi:/var/mqsi:Z \
    -v /host/overrides:/mnt/overrides:Z \
    your-ace-image

